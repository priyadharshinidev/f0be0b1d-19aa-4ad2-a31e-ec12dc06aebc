<Project Sdk="Microsoft.NET.Sdk">
  <!-- Builds the command-line application that hosts the LIS solver. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>
  <ItemGroup>
    <!-- The CLI currently has no external package dependencies. -->
  </ItemGroup>
</Project>
